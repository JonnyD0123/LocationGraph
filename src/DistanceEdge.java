public class DistanceEdge {
    LocationVertex vertex;
    double distance;

    public DistanceEdge(LocationVertex vertex, double distance) {
        this.vertex = vertex;
        this.distance = distance;
    }
}
